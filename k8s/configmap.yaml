apiVersion: v1
kind: ConfigMap

metadata:
  name: devtrack-config
  namespace: devtrack

data:
  PORT: "5000"

  MONGO_HOST: "mongodb"
  MONGO_PORT: "27017"
  MONGO_DATABASE: "devtrack"

  NODE_ENV: "production"